version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - ASAAS_BASE_URL=https://sandbox.asaas.com/api/v3
      - ASAAS_API_KEY=$<YOUR_API_KEY>
